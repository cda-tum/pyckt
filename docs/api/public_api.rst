Public API (``pyckt``)
======================

The six analysis modes as plain, typed functions.  Each is re-exported at the
top level, so ``from pyckt import recognize`` and ``from pyckt.api import
recognize`` are equivalent.

.. automodule:: pyckt.api
   :members:
